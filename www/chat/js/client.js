"use strict";
//

/** @type {Map<String,Function} */
export const commands = new Map();

//
commands.set("shrug", (v) => {
    return v + " ¯\\_(ツ)_/¯";
});
commands.set("tableflip", (v) => {
    return v + " (╯°□°）╯︵ ┻━┻";
});
